// app/dashboard/profile/layout.tsx
"use client"; // marks all child components in this directory as client components


const ProfileLayout = ({ children }) => {
  return <>{children}</>; // Render the children components
};

export default ProfileLayout;
